[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "drover-executor"
version = "0.1.0"
description = "Worker-agent library for Drover workers"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
drover-executor = "drover_executor.__main__:main"
