// Package api is the HTTP client over the orchestrator REST API. It holds the
// Client struct, request plumbing, the bearer-auth header, the shared
// request/response types, and typed error decoding. (Scaffolding only; the
// client lands in a later phase.)
package api
