published: 0.0.19
changes:
- version: 0.0.19
  date: '2026-05-30'
  entries:
  - bump: patch
    description: |
      Give each worker its own socket folder instead of a single
      socket file. The orchestrator now creates a per-worker folder
      `{socket_dir}/{worker_id}/` containing the control socket
      `orchestrator.sock`, and bind-mounts that folder into the
      worker at `/var/run/drover/sockets/` (previously a single
      socket file was bound at `/run/orchestrator.sock`). The folder layout
      makes room for multiple sockets per worker later, e.g. one per
      interactive shell. This is a breaking change to the orchestrator/worker
      socket contract; Drover is pre-production so no backwards
      compatibility is provided.
- version: 0.0.18
  date: '2026-05-27'
  entries:
  - bump: patch
    description: |
      Enforce the `drover.capabilities` image label: exec requests are rejected
      with 422 unless the image declares the `exec` capability. An absent/empty
      label or an unresolvable image is denied rather than assumed.
- version: 0.0.17
  date: '2026-05-24'
  entries:
  - bump: patch
    description: |
      Release to trigger new umbrella release strategy
- version: 0.0.16
  date: '2026-05-22'
  entries:
  - bump: patch
    description: |
      Add a per-worker WebSocket endpoint at `GET /workers/{id}/ws` that
      streams both exec command output and the worker's Docker logs in real
      time. Replaces polling for long-running commands. Messages are JSON
      objects tagged with `type: "output" | "status" | "log" | "error"`;
      exec messages carry the `command_id` so clients can correlate with the
      REST exec API. Multiple WebSocket clients per worker are supported;
      each gets an independent 256-message queue with drop-on-full semantics
      so a slow consumer can't back-pressure the orchestrator.

      Auth uses the same Bearer token as the REST API (header during the HTTP
      upgrade) with a `?token=` query-param fallback for browser clients that
      can't set custom headers. A `?tail=<N>` parameter replays the last N
      Docker log lines before live-tailing.

      Operator note: any reverse proxy in front of the orchestrator must
      forward the `Upgrade` and `Connection` headers and speak HTTP/1.1
      upstream. Caddy and Traefik handle this automatically; nginx needs the
      standard three-line config. See the orchestrator README for the full
      message schema and a minimal Python client.
- version: 0.0.15
  date: '2026-05-21'
  entries:
  - bump: patch
    description: |
      Add startup warning when gVisor (`runsc`) is registered in Docker but the
      required `--host-uds=all` flag is absent from its `runtimeArgs`. Without
      that flag, gVisor blocks the worker agent inside every non-privileged
      worker from connecting to its orchestrator socket, causing all
      workers to time out with `init_timeout`. The orchestrator now inspects
      the Docker daemon's runtime configuration at startup and logs a clear,
      actionable warning if the flag is missing.

      Also logs both the in-container and host-side socket paths when launching a
      worker, making path-resolution issues easier to diagnose.
  - bump: patch
    description: |
      Fix `docs/install-runsc-gvisor.md` to include `--host-uds=all` in the
      required `daemon.json` config. This flag was documented in
      `docs/full-e2e-suite.md` but missing from the operator install guide,
      causing operators following that guide to hit `init_timeout` failures on
      all non-privileged workers with no obvious error message.
  - bump: patch
    description: |
      `POST /workers/{id}/resume` now mirrors the `POST /workers` init
      flow: it returns immediately with status `resuming`, then transitions
      to `running` only after Docker starts the worker AND the worker agent
      reconnects to the orchestrator socket and re-sends a `ready` message.
      Previously the row flipped to `running` as soon as `docker start`
      returned, leaving a window where the API reported the worker as
      running but exec calls failed with `409 WorkerNotConnected`.

      A new resume watchdog fires `DROVER_INIT_TIMEOUT_SECONDS` (the existing
      knob is reused) after the resume request and transitions the row to
      `error` if `ready` never arrives. Two new `error_code` values are set
      by the new failure paths:

      - `resume_docker_error` — Docker start call failed during resume.
      - `resume_timeout` — worker agent did not reconnect/send `ready` in time.

      The existing `orchestrator_crash` error code now also covers rows
      interrupted while in `resuming` (previously only `initializing` was
      handled by startup reconciliation).
- version: 0.0.14
  date: '2026-05-21'
  entries:
  - bump: patch
    description: |
      Fix builder (worker) launch failure where the executor could not
      connect to `/run/orchestrator.sock`. The socket bind-mount source was using
      the in-container path for the sockets directory, but Docker resolves bind
      sources against the host filesystem. This worked when the sockets directory
      was mounted at the same path on both sides, but broke when the
      `docker-compose.yml` default changed to `./sockets` on the host. The
      orchestrator now self-inspects its own mounts to resolve the host-side
      sockets directory path, mirroring how the host Docker socket path is
      already resolved.
- version: 0.0.13
  date: '2026-05-20'
  entries:
  - bump: patch
    description: |
      Fix startup crash on fresh deploys where the `./data` or `./logs` host
      directories don't exist yet. Docker creates missing bind-mount source
      directories as root-owned, which the orchestrator user (UID 1000) cannot
      write to. The entrypoint now chowns the data and logs directories at
      startup alongside the existing sockets-directory fix.
- version: 0.0.12
  date: '2026-05-19'
  entries:
  - bump: patch
    description: |
      Standardize the container-internal paths and host-bind contract.

      In-container paths are now fixed and not configurable:
      `/var/lib/drover/data/db.sqlite` (SQLite + future config),
      `/var/lib/drover/logs/` (captured worker logs when
      `DROVER_ENABLE_WORKER_LOGS=true`), `/var/run/docker.sock`
      (host Docker daemon), and `/var/run/drover/sockets/` (per-
      worker Unix sockets). Removed the path-override env
      vars `DROVER_DB_PATH`, `DROVER_SOCKET_DIR`, and
      `DROVER_DOCKER_SOCK` from the orchestrator.

      Privileged-worker bind fix: previously the orchestrator used
      its in-container view of the Docker socket
      (`/var/run/docker.sock`) as the bind source when launching
      privileged workers, which silently broke under rootless
      Docker (the host socket lives at `/run/user/$UID/docker.sock`,
      so Docker resolved the bind source against a non-existent host
      path and bound an empty directory at `/run/docker.sock` inside
      the privileged worker). The orchestrator now
      self-inspects through the Docker API at startup, reads the
      `Source` of its own `/var/run/docker.sock` mount, and uses that
      host path as the bind source. No new environment variable is
      required.

      Sample `docker-compose.yml` now uses host bind mounts under
      `./data` and `./logs` (overridable via `DROVER_DATA_DIR` and
      `DROVER_LOGS_DIR` compose-level env vars) and a host-side
      `DROVER_HOST_DOCKER_SOCK` for the Docker socket. The
      per-worker sockets directory is bound at
      `/var/run/drover/sockets:/var/run/drover/sockets` (the host
      path must match the container path because Docker resolves
      nested bind-mount sources against the host filesystem). The
      orchestrator entrypoint chowns that directory to UID 1000
      on startup, so no host-side pre-creation is required.
- version: 0.0.11
  date: '2026-05-18'
  entries:
  - bump: patch
    description: |
      Renamed exec route path segments from `/exec` to `/execs` for consistency
      with the plural `/workers` convention. Callers must update:
      - `POST /workers/{id}/exec` → `POST /workers/{id}/execs`
      - `GET /workers/{id}/exec/{cmd_id}` → `GET /workers/{id}/execs/{cmd_id}`
  - bump: patch
    description: |
      New `GET /workers/{id}/execs` endpoint returns a list of every command
      that has been submitted against a worker (newest first), powering the
      new exec command panel in the webapp. `GET /workers/{id}/execs/{cmd_id}`
      also now includes the original `command` string in its response.
  - bump: patch
    description: |
      Detection of the orchestrator's own Docker container id is now much
      more robust. The previous regex only matched cgroupfs-driver paths
      (`/<hex>` or `/<hex>.scope`), so on every other Docker × cgroup
      combination — including the systemd driver default on modern Ubuntu
      and GitHub Actions — detection silently returned `None` and
      `GET /workers/{id}/logs/orchestrator` returned 503.

      Detection now tries three independent signals in order:
      (1) `/proc/self/cgroup` — strict `[/-]<hex>(.scope)?` match per line,
      then a loose 64-hex scan for nested kubernetes / containerd layouts;
      (2) `/proc/self/mountinfo` — every Docker container has
      `/var/lib/docker/containers/<id>/{hostname,hosts,resolv.conf}`
      bind-mounted in, leaving an unmistakable `/containers/<id>/`
      substring that is independent of cgroup driver and hostname config;
      (3) container hostname — short 12-hex id when nothing else matches,
      skipped when the operator (or Compose) has overridden hostname to a
      non-id value such as a service name.

      When detection still fails the startup warning now includes the
      actual hostname, `/proc/self/cgroup`, and `/proc/self/mountinfo`
      snapshots so the failure mode can be diagnosed from the orchestrator
      log alone.
- version: 0.0.10
  date: '2026-05-14'
  entries:
  - bump: patch
    description: |
      New `GET /workers/{id}/logs/orchestrator` endpoint returns the
      orchestrator's own structured log lines filtered to a single worker,
      making it easy to see lifecycle events for one worker without reading
      the full orchestrator log.
  - bump: patch
    description: |
      Captured worker stdout/stderr is now retained on disk
      under `DROVER_LOG_DIR` and reachable through
      `GET /workers/{id}/logs/files` (and `/logs/files/{filename}`)
      until the worker is destroyed. See
      [`docs/observability.md`](../docs/observability.md) for the full
      retention model, on-disk format, and log-shipper recipes.
- version: 0.0.9
  date: '2026-05-13'
  entries:
  - bump: patch
    description: |
      Validating release and container issues.
- version: 0.0.8
  date: '2026-05-11'
  entries:
  - bump: patch
    description: |
      Switched image discovery from the `drover/` name prefix to Docker
      labels: an image is now considered Drover-managed when it carries
      `drover.managed=true` and `drover.name=<name>`. Image responses
      additionally expose a `labels` field carrying the full `drover.*`
      label set (filtered so non-Drover labels don't leak), and the
      `tags` field lists the underlying Docker tags for information only.
- version: 0.0.7
  date: '2026-05-10'
  entries:
  - bump: patch
    description: |
      Bump the Docker API version.
- version: 0.0.6
  date: '2026-05-10'
  entries:
  - bump: patch
    description: |
      Handle the rootless-Docker case where `/var/run/docker.sock` doesn't
      exist on the host. Docker silently materialises the missing source path
      as an empty directory inside the container, so the previous fix saw a
      non-socket and the call failed with EACCES.

      The entrypoint now distinguishes "missing path", "directory" and
      "non-socket" cases at startup and prints a hint pointing rootless users
      at `$XDG_RUNTIME_DIR/docker.sock`. The 503 response body from the
      Docker exception handler stats the configured socket and includes the
      same hint, so it shows up in the webapp UI. `docker-compose.yml` now
      bind-mounts `${DOCKER_SOCK:-/var/run/docker.sock}` and documents the
      rootless override inline.
- version: 0.0.5
  date: '2026-05-10'
  entries:
  - bump: patch
    description: |
      Fix `Permission denied` when the orchestrator container connects to the
      Docker socket. The entrypoint now starts as root, joins the `orchestrator`
      user to the socket's group at runtime, then drops privileges via `gosu`.
      Also adds FastAPI exception handlers for `httpx.RequestError` and
      `DockerError` so socket / Docker failures surface as 503/502 responses
      with a useful `detail` body instead of a bare 500.
- version: 0.0.4
  date: '2026-05-10'
  entries:
  - bump: patch
    description: "Continued release testing. \n"
- version: 0.0.3
  date: '2026-05-09'
  entries:
  - bump: patch
    description: "Continued testing of releases. \n"
- version: 0.0.2
  date: '2026-05-09'
  entries:
  - bump: patch
    description: |
      Continued testing of releases.
- version: 0.0.1
  date: '2026-05-09'
  entries:
  - bump: patch
    description: |
      Adding automated releases via change files.
